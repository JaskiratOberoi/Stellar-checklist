# Production build: absolute API origin (the Hostinger site and the API are different origins).
# Leave empty in development; vite proxies /api to the dev API.
VITE_API_URL=https://sms-api.genomicslab.in
